fastapi>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0
httpx>=0.25.0
celery>=5.3.0
redis>=5.0.0
minio>=7.2.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
aiofiles>=23.2.0
tuspy>=1.0.0
playwright>=1.45.0
pandas>=2.1.0
openpyxl>=3.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 与 bcrypt>=4.1 不兼容（72 字节检测会抛错），锁定兼容版本
bcrypt<4.1
# 数据库迁移（Alembic 集成）
alembic>=1.13.0
# RPA Cookie AES-256 加密（Fernet）
cryptography>=41.0.0
# 竖屏转横屏智能裁切（OpenCV 人脸检测 / 动态跟踪）
opencv-python-headless>=4.8.0
# 去水印（v4）：remove-ai-watermarks 两套引擎共用视频可见水印清除能力
# lama/migan extra 提供 onnxruntime CPU 高质量修补（LaMa-ONNX ~200MB / MI-GAN-ONNX ~28MB），
# 无需 GPU 即可显著优于 cv2 经典修补；backend=auto 时按 LaMa > MI-GAN > cv2 自动择优
remove-ai-watermarks[visible,video,lama,migan]>=0.26.0
# 去水印（v5）：seedance_wm 引擎（集成自 ben500500/remover 仓库的 5 阶段流水线）依赖
# ffmpeg-python：FFmpeg I/O 绑定（抽帧/合成）；PyYAML：YAML 配置加载
ffmpeg-python>=0.2.0
PyYAML>=6.0
# seedance_wm LaMa 修补使用 remove-ai-watermarks 的 erase_lama（onnxruntime，已安装）
# 无需额外安装 torch 或 iopaint
fonttools>=4.40.0  # 固定文字字体方案C：从 .ttc 提取 SC face 单字体（绕开 JP 字形歧义）

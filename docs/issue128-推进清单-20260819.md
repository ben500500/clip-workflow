@cnb.c8egcIqXAIA(Benny) 收到，我把当前 ISSUE #128 里**所有"还需更进一步"的事项**拉成一张可执行的清单，按「是否阻塞 / 依赖什么 / 谁来动」标注，方便你直接拍板下一步。

---

# 📋 需要更进一步的事项清单

## 🔴 第一优先级（当前卡点）

| # | 事项 | 现状 | 还需做什么 |
|---|------|------|-----------|
| **A1** | **PR #206（Batch-A）评审与合入** | ✅ **已合入**（`f34d43f`，经 PR #207 合并带上 main） | 已由 DSH 主控评审通过（三阶段拆分逐段对照等价，output_id 改写经返回值传回、7 config 字典传递无缺漏）；**待部署 + 切片三路冒烟** |
| **A2** | **真实环境冒烟（放行前提）** | ⏳ 部分完成 | ✅ 被动验证已过（579d6c4 部署后 `exc_type KeyError` 噪音 0 次）；待 251fbe2 部署后执行：①切片三路回归（普通/快速转换/重新剪辑）②发布失败主动验证（DB 状态=failed + 日志无 exc_type） |

## 🟡 第二优先级（Batch-A 收尾遗留项）

| # | 事项 | 现状 | 还需做什么 |
|---|------|------|-----------|
| **B1** | **#11 宽异常收敛** | ⏳ 已延后 | 等 Codex 报告里的**具体条目定位**再精准收窄，避免过度收敛回归（PR #206 已说明，暂不并入） |
| **B2** | **#9 剩余路由普查** | ⏳ 部分完成 | 只删了 `run_preview`/`engines/preview.py`；剩余候选需**更严谨的调用链验证**（`graft callers`）再删，防误删在用路由 |

## 🔵 第三优先级（Codex 报告 11 条剩余项，排期执行）

| # | 事项 | 均分 | 排期 | 说明 |
|---|------|------|------|------|
| **C1** | **#6 队列分 worker** | 8.5 | Phase-2 | 选点/切片/发布独立容器，消除饥饿 |
| **C2** | **#4 sliceConfig 收敛** | 7.5 | Phase-2（前端错峰） | 前端 3 配置页，须避开并行 pipeline 活跃期 |
| **C3** | **#5 Playwright 统一** | 8.0 | Batch-B / Phase-3 | 进程级单例 + 引用计数 + 空闲回收 |
| **C4** | **#2 tasks.py 拆分** | 7.5 | Batch-B / Phase-4 | 保守重构，硬约束 `app.celery.tasks.X` 任务名不变 |
| **C5** | **#8** | 6.5 | 排期 | 待定 |

## ⚪ 可选迭代（非阻塞，按需做）

| # | 事项 | 说明 |
|---|------|------|
| **D1** | **选点并发限制落地成机制** | 目前「只跑 1-2 个 GPU 任务」仍是口头约束，建议加 `gpu_concurrency` / 选点并发字段做限流 |
| **D2** | **前端 pipeline_mode 展示** | 解耦模式已有开关，前端可加状态展示便于观察 |
| **D3** | **#205 boundary_refine 前端配置入口** | 选点结尾优化已落地，若要让运营可调吸附窗口/静音阈值需补前端入口 |

---

## 📌 我建议的下一步推进顺序

1. **A1 → 评审 PR #206**（Batch-A 合入，核心路径行为一致性最需要人肉复核）
2. **A2 → 运维冒烟**（放行后续所有 Phase-2/3 批次的前提）
3. **B1/B2 → 拿精确项后小步提交**（#11、#9 剩余，各自单独 PR 降风险）
4. **C1~C5 → 按 Phase-2 → Batch-B 错峰推进**（前端项避开并行活跃期）

---

**要我接着做哪一步？** 比如：
- 给 PR #206 的 #7 出一份「拆分前后逐段对照」评审辅助清单
- 用 `graft callers` 把 #9 剩余路由的调用链核实一遍、给出可安全删除的候选
- 先推进 C1（#6 队列分 worker）或其他你更优先的项

你定个优先级，我直接动手。
# clip-workflow 代码治理 · 推进资料包

> 整理日期：2026-08-19（凌晨，服务器时间 2026-08-19 07:xx）
> 用途：为 ISSUE #128 的后续推进（Batch-A/B/C + 运维验证）提供一站式参考
> 状态基准：`local = cnb = 163 = 579d6c4`（已部署，38+ 分钟无异常）

---

## 一、状态总览（当前快照）

| 项 | 状态 |
|----|------|
| 代码 HEAD | `579d6c4`（含 #1 Celery 修复、#3 死代码、#205 选点结尾优化、#203 clean-dead-fail、剧目系列 MR#200-202） |
| 163 部署 | ✅ 已部署 579d6c4，容器全健康，exc_type 日志噪音 0 次 |
| 在途 PR | **PR #206**（`auto/refactor-slice-dead-a1b2`）：run_slice 拆分三阶段 + run_preview 死链删除 + shortdrama FAILURE 残留——**已评审通过，待合入** |
| 待办主线 | 审核材料.md 的 A1（合入）→ A2（主动冒烟）→ B1/B2 → C1-C5（Phase-2/3/4） |

## 二、核心文档清单

| 文档 | 路径 | 用途 |
|------|------|------|
| **Codex 审查报告（主参考）** | `docs/codex-review-report-20260819.md` | 11 条问题 + Top10 + 修改方案；**第 6 节为 DSH 主控复核意见**（核验/排序/风险） |
| **执行清单（Benny/cnb）** | `docs/issue128-推进清单-20260819.md` | A/B/C/D 分级推进表，当前唯一在途 PR = #206 |
| **审查提示词（graft 优先）** | `docs/codex-审查提示词-graft优先.md` | 下一轮 Codex 审查直接复用 |
| **运维交接文档** | `clip-workflow交接文档-20260818.md`（仓库外，工作区根） | 部署链路/SYNC_DIRS/alembic 禁忌/运维边界 |
| **运维交接文档** | `dsh-workspace/clip-workflow交接文档-20260818.md` | 部署链路/SYNC_DIRS/alembic 禁忌/运维边界 |

## 三、仓库 docs/ 设计文档（评审对照"设计意图"时用）

- `docs/视频号多运营者发布并入clip-workflow-设计方案-20260814-v3.1定稿.md` —— 多运营者/登录态/配额设计
- `docs/多视频号素材去重-设计方案-圆桌定稿-20260817.md` —— 去重变体设计
- `docs/视频号素材导入下载-立项技术设计方案-20260816.md` —— 素材下载解析
- `docs/剧目管理设计方案-20260818.md` —— 剧目库（MR#200-202）
- `docs/AUTOMATION_WORKFLOW.md`（v1.2）+ `docs/自动化产线契约对齐测试报告-20260818.md` —— 14 步产线契约
- `docs/DEPLOYMENT.md` / `docs/remote-worker-部署说明.md` —— 部署/远程 worker

## 四、已完成事项（可核对的证据）

| # | 事项 | 证据/验证 |
|---|------|----------|
| #1 | Celery 失败序列化修复（14 处 FAILURE 双写移除） | 已合入 `3cde54c`；部署后 `exc_type` 日志 **0 次**（修复前每次部署必现） |
| #3 | workflow-status 死代码删除（后端 184 行 + 前端封装） | 已合入 `1e35776`；`graft grep workflow-status` 应为 0 |
| #10 | graft 入库策略（`.gitignore` 移除整目录忽略，保留 .cache/.graph） | 已落地 `a7013bb`（Codex 执行，本 agent 复核认可） |
| A1 | PR #206 评审 + 合入 + 部署 | ✅ **通过并上线**：三阶段拆分等价（output_id 改写经返回值传回，7 config 字典传递无缺漏）；`f34d43f` 合入 → 部署 `0a35e1f` |
| B2 | run_preview 死链删除安全性 | ✅ 独立验证：`engines/preview.py` 0 import 引用，`slice_service.run_preview` 0 调用 |
| A2 | 冒烟（被动 + 快速转换实测） | ✅ exc_type 噪音 **0 次**（修复前每次部署必现）；no_cut 实测任务 `d147df5b` completed 100% 出片，worker 无异常；剩余：发布失败主动验证 / 普通切片·重新剪辑分支随自然使用覆盖 |

## 五、近期合入功能/修复（背景参考）

- **MR #203**：clean-dead-fail（死代码+失败路径清理汇总）
- **MR #205**：选点结尾优化（提示词断点优先 + 静音边界吸附 boundary_refine）
- **MR #200/#201/#202**：剧目库（后端+前端+剧集维度切片状态聚合）+ 发布弹窗选剧目生成素材 + MissingGreenlet 修复
- **MR #196/#199**：切片收尾 / 封面首帧修复
- 本日早前：一键切片后端兜底（auto_autoclip_if_empty）、批量切片配置（预设/时长/封面/去重档位）、源视频预览

## 六、下一步推进（对齐审核材料顺序）

| 序 | 动作 | 负责人建议 | 验收信号 |
|----|------|-----------|----------|
| 1 | **合入 PR #206** → 部署 163 | cnb 合入 → DSH 部署 | 部署无错误日志 |
| 2 | **切片链路冒烟**（普通/快速转换/重新剪辑三路） | DSH | 三路任务正常出片 |
| 3 | **A2 主动验证**：造发布失败场景 | DSH | DB 状态=failed、日志无 exc_type |
| 4 | B1：#11 宽异常精确项（graft grep except Exception 300 处收敛抽查） | cnb | 精确清单后小步提交 |
| 5 | B2：#9 剩余路由普查（graft callers 逐路由 in-edges） | cnb | 0 调用端点清单 |
| 6 | C1：#6 队列分 worker（selection 独立容器） | 排期 Phase-2 | 长切片不阻塞选点 |
| 7 | C2：#4 sliceConfig 收敛（前端错峰） | 排期 Phase-2 | 三页面单一配置模型 |
| 8 | C3：#5 Playwright 统一 / C4：#2 tasks.py 拆分 / C5：#8 | 排期 Batch-B/Phase-4 | 任务名不变等硬约束 |

## 七、运维速查（推进时随手用）

```bash
# 部署（本机仓库，工作树必须干净）
bash scripts/deploy_server.sh
# 部署后冒烟：exc_type 噪音检查
docker compose logs --since 30m worker-publish backend | grep -cE "exc_type|Exception information"
# PR #206 分支对照（评审用）
git diff HEAD...cnb/auto/refactor-slice-dead-a1b2 --stat
# 死代码普查
graft callers <符号>   # in-edges=0 即候选
# 地图刷新（改完代码必做，别忘了恢复 .gitignore）
graft build && git checkout -- .gitignore && git add graft/ && git commit
```

## 八、风险提示（执行时盯着）

1. **#2 tasks.py 拆分**：任务完整名 `app.celery.tasks.X` 不可变，否则 in-flight 任务丢失（硬约束）
2. **#4 sliceConfig 收敛**：先与并行 pipeline 对齐配置字段清单再动，避免互踩
3. **任何重构合入后**：必须 `graft build` 刷新地图并提交（防漂移）
4. **Codex 有越权倾向**（上轮自行提交 a7013bb）：指派只读任务时明确禁止 git 写操作
5. 大重构（C4/C5）选并行 pipeline 低活跃时段执行，分两次合入
